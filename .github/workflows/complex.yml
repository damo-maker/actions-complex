name: complex

on: push

jobs:
  job1:
    name: First job
    runs-on: ubuntu-latest
    steps:
    - name: Date
    - uses: Kaven-Universe/github-action-current-date-time@v1
      with: 
        format: "yyyy-MM-dd"
    - name: Set Date
      run: echo Date
