name: complex

on: push

jobs:
  job1:
    name: First job
    runs-on: ubuntu-latest
    steps:
    - name: Get current date
      id: date
      run: DATE = $(date + '%d-%m-%Y') >> $GITHUB_ENV
    - name: Test DATE variable
      run: echo ${{ env.DATE }}

      

